
<div class="refrence-section">
  <div class="reference-heading">{{ section.settings.heading }}</div>
  <p>{{ section.settings.subheading }}</p>

  <div class="referencelogo">
    {% for block in section.blocks %} 
    <div class="refdiv">
    <img class="lazyload" src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" data-src="{{ block.settings.logo | img_url: '250x250' }}">
    <p>{{ block.settings.label }}</p>
    </div>
      {% endfor %}
  </div>
</div>

{% schema %}
  {
    "name": "Refrence Section",
    "class":"reference",
    "settings": [
    {
     "type":"text",
     "id":"heading",
     "label":"Heading"
    },
     {
     "type":"text",
     "id":"subheading",
     "label":"Subheading"
    }
],
"blocks":[{
	"name": "logo",
	"type": "logos",
	"settings":[
      {
       "type": "image_picker",
       "id": "logo",
       "label": "Add image"
        },
      {
        "type":"text",
        "id":"label",
        "label":"logo label"
      }
]
}],
"presets":[
 {
   "name":"Reference"
 }
]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
