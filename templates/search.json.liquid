{% layout none %}
{ "products": [ {% paginate search.results by 1000 %} {% assign itms = search.results | reverse %} {% for item in itms %}
{
	"id" : "{{ item.id }}",
	"title": "{{ item.title | escape}}",
	"handle": "{{ item.handle }}",
	"name": "{{ item.metafields.my_fields.product_model | escape }}"
}{% unless forloop.last %},{% endunless %}
{% endfor %} {% endpaginate %} ]}