<!doctype html>
<html class="no-js for-font" lang="{{ request.locale.iso_code }}">
  <head>


    <!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=AW-489208709"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'AW-489208709');
</script>


    
    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | img_url: '32x32' }}">
    {%- endif -%}
    {% if page_description %}{%- comment smartseo_integration_begin_meta_data -%}<meta name="description" content="{{ page_description | escape }}">{%- endcomment smartseo_integration_end_meta_data -%}{% endif %}

  
    <meta name="viewport" content="width=device-width,initial-scale=1">
     

    {% render 'meta-tags' %}
    
    {% unless template contains 'cart' %}
      <script src="https://rapidlercdn.com/6426631c44f2512c1cbbd64f/preload.js"></script>
    {% endunless %}

<link rel='preconnect dns-prefetch' href='https://triplewhale-pixel.web.app/' crossorigin />
<link rel='preconnect dns-prefetch' href='https://api.config-security.com/' crossorigin />
<script>
/* >> TriplePixel :: start*/
~function(W,H,A,L,E,_,B,N){function O(U,T,H,R){void 0===R&&(R=!1),H=new XMLHttpRequest,H.open("GET",U,!0),H.send(null),H.onreadystatechange=function(){4===H.readyState&&200===H.status?(R=H.responseText,U.includes(".txt")?eval(R):N[B]=R):(299<H.status||H.status<200)&&T&&!R&&(R=!0,O(U,T-1))}}if(N=window,!N[H+"sn"]){N[H+"sn"]=1;try{A.setItem(H,1+(0|A.getItem(H)||0)),(E=JSON.parse(A.getItem(H+"U")||"[]")).push(location.href),A.setItem(H+"U",JSON.stringify(E))}catch(e){}A.getItem('"!nC`')||(A=N,A[H]||(L=function(){return Date.now().toString(36)+"_"+Math.random().toString(36)},E=A[H]=function(t,e){return W=L(),(E._q=E._q||[]).push([W,t,e]),W},E.ch=W,B="configSecurityConfModel",N[B]=1,O("//conf.config-security.com/model",0),O("//triplewhale-pixel.web.app/triplefw.txt?",5)))}}("","TriplePixel",localStorage);
/* << TriplePixel :: end*/
</script>
   <!-- Added by AVADA Cookies Bar -->
   {% include 'avada-cookies-bar' %}
   <!-- /Added by AVADA Cookies Bar -->
 

  <!-- Google Tag Manager -JLM -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
       new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
       j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
       'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
       })(window,document,'script','dataLayer','GTM-W6CWKJR');</script>
    <!-- End Google Tag Manager -->
    
    <!-- Start VWO Async SmartCode -->
    <script type="text/javascript" id="vwoCode">
      window._vwo_code=window._vwo_code || (function() {
      var account_id=685892,
      version = 1.5,
      settings_tolerance=2000,
      library_tolerance=2500,
      use_existing_jquery=false,
      is_spa=1,
      hide_element='body',
      hide_element_style = 'opacity:0 !important;filter:alpha(opacity=0) !important;background:none !important',
      /* DO NOT EDIT BELOW THIS LINE */
      f=false,d=document,vwoCodeEl=d.querySelector('#vwoCode'),code={use_existing_jquery:function(){return use_existing_jquery},library_tolerance:function(){return library_tolerance},hide_element_style:function(){return'{'+hide_element_style+'}'},finish:function(){if(!f){f=true;var e=d.getElementById('_vis_opt_path_hides');if(e)e.parentNode.removeChild(e)}},finished:function(){return f},load:function(e){var t=d.createElement('script');t.fetchPriority='high';t.src=e;t.type='text/javascript';t.innerText;t.onerror=function(){_vwo_code.finish()};d.getElementsByTagName('head')[0].appendChild(t)},getVersion:function(){return version},getMatchedCookies:function(e){var t=[];if(document.cookie){t=document.cookie.match(e)||[]}return t},getCombinationCookie:function(){var e=code.getMatchedCookies(/(?:^|;)\s?(_vis_opt_exp_\d+_combi=[^;$]*)/gi);e=e.map(function(e){try{var t=decodeURIComponent(e);if(!/_vis_opt_exp_\d+_combi=(?:\d+,?)+\s*$/.test(t)){return''}return t}catch(e){return''}});var i=[];e.forEach(function(e){var t=e.match(/([\d,]+)/g);t&&i.push(t.join('-'))});return i.join('|')},init:function(){if(d.URL.indexOf('__vwo_disable__')>-1)return;window.settings_timer=setTimeout(function(){_vwo_code.finish()},settings_tolerance);var e=d.createElement('style'),t=hide_element?hide_element+'{'+hide_element_style+'}':'',i=d.getElementsByTagName('head')[0];e.setAttribute('id','_vis_opt_path_hides');vwoCodeEl&&e.setAttribute('nonce',vwoCodeEl.nonce);e.setAttribute('type','text/css');if(e.styleSheet)e.styleSheet.cssText=t;else e.appendChild(d.createTextNode(t));i.appendChild(e);var n=this.getCombinationCookie();this.load('https://dev.visualwebsiteoptimizer.com/j.php?a='+account_id+'&u='+encodeURIComponent(d.URL)+'&f='+ +is_spa+'&vn='+version+(n?'&c='+n:''));return settings_timer}};window._vwo_settings_timer = code.init();return code;}());
    </script>
    <!-- End VWO Async SmartCode -->
    

    <!-- Added by AVADA SEO Suite -->
    {% include 'avada-seo' %}
    <!-- /Added by AVADA SEO Suite -->
    {% render 'structuredDataV2' %}
    {% render 'SEO-Master' %}
    {% render 'preload' %}
    <link rel="canonical" href="{{ canonical_url }}">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="author" content="Syahrizaldev">
    <meta name="theme-color" content="">

    {% if template == 'collection' %}
      <META NAME="robots" CONTENT="index,follow,max-snippet:-1,max-video-preview:-1,max-image-preview:large">
    {% elsif template == 'cart' %}
      <META NAME="robots" CONTENT="index,follow,max-snippet:-1,max-video-preview:-1,max-image-preview:large">
    {% elsif template == 'search' %}
      <META NAME="robots" CONTENT="index,follow,max-snippet:-1,max-video-preview:-1,max-image-preview:large">
    {% elsif template contains 'customers' %}
      <META NAME="robots" CONTENT="index,follow,max-snippet:-1,max-video-preview:-1,max-image-preview:large">
    {% endif %}

    {{ 'libs.js' | asset_url | script_tag }}
    {{ 'libs.css' | asset_url | stylesheet_tag }}

    <!-- Hotjar Tracking Code for WWW.OXALEATHER.COM -->
    <script>
      (function(h,o,t,j,a,r){
       h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
       h._hjSettings={hjid:2490627,hjsv:6};
       a=o.getElementsByTagName('head')[0];
       r=o.createElement('script');r.async=1;
       r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
       a.appendChild(r);
      })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');
    </script>

    <!-- GA Additional Tracking Code for WWW.OXALEATHER.COM -->
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
       m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
       ga('create', 'UA-182881503-1', 'auto'); // Insert your GA Web Property ID here, e.g., UA-12345-1
      //////////// New lines Added////////////////////
       ga('set','dimension1','shopify_US_6246957875398_40880764059846'); // REQUIRED Item ID value, e.g., 12345, 67890
      ///////////////////////////////////////////////
       ga('send', 'pageview');
    </script>
    <!-- GA Additional Tracking Code for WWW.OXALEATHER.COM -->

    
      <script defer src='https://code.buywithprime.amazon.com/default.variant.selector.shopify.js'></script>
      <script async fetchpriority='high' src='https://code.buywithprime.amazon.com/bwp.v1.js'></script>
  
    <!-- Google Optimize Tag-JLM -->
    <script src="https://www.googleoptimize.com/optimize.js?id=OPT-NX2SDBZ" defer></script>
    <!-- END Google Optimize Tag -->

    <!-- JLM Analytics Code snippet -->
    <script
      data-host="https://analytics.justleadmarket.com/public"
      data-dnt="false"
      src="https://analytics.justleadmarket.com/public/js/script.js"
      id="ZwSg9rf6GA"
      async
      defer
    ></script>
    <!-- END JLM Analytics Code snippet -->

    {%- unless settings.type_header_font.system? and settings.type_body_font.system? -%}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
    {%- endunless -%}

    {%- comment smartseo_integration_begin_meta_data -%}<title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>{%- endcomment smartseo_integration_end_meta_data -%}

    <script src="{{ 'global.js' | asset_url }}" defer="defer"></script>
    {% render 'oxa' %}
    {% comment %}
      {% include 'meteor-menu', load: 'head' %}
      {{ content_for_header }}
    {% endcomment %}
    {%- liquid
      assign body_font_bold = settings.type_body_font | font_modify: 'weight', 'bold'
      assign body_font_italic = settings.type_body_font | font_modify: 'style', 'italic'
      assign body_font_bold_italic = body_font_bold | font_modify: 'style', 'italic'
    %}

    {% style %}{% render 'theme' %}{% endstyle %}

    {{ 'base.css' | asset_url | stylesheet_tag }}
    {{ 'section-footer.css' | asset_url | stylesheet_tag }}

    <link rel="stylesheet" href="{{ 'component-newsletter.css' | asset_url }}" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
    <link
      rel="stylesheet"
      href="{{ 'component-list-payment.css' | asset_url }}"
      media="print"
      onload="this.media='all'"
    >
    <link rel="stylesheet" href="{{ 'component-list-social.css' | asset_url }}" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="{{ 'component-rte.css' | asset_url }}" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="{{ 'disclosure.css' | asset_url }}" media="print" onload="this.media='all'">
    <link
      rel="prerender"
      href="https://k72b1ne8bjmgvrbc-50704187590.shopifypreview.com/products/iphone-14-plus-leather-wallet-case"
    >
    <link
      rel="preconnect"
      href="https://k72b1ne8bjmgvrbc-50704187590.shopifypreview.com/products/iphone-14-plus-leather-wallet-case"
    >
    <link
      rel="dns-prefetch"
      href="https://k72b1ne8bjmgvrbc-50704187590.shopifypreview.com/products/iphone-14-plus-leather-wallet-case"
    >
    <noscript>{{ 'component-newsletter.css' | asset_url | stylesheet_tag }}</noscript>
    <noscript>{{ 'component-list-menu.css' | asset_url | stylesheet_tag }}</noscript>
    <noscript>{{ 'component-list-payment.css' | asset_url | stylesheet_tag }}</noscript>
    <noscript>{{ 'component-list-social.css' | asset_url | stylesheet_tag }}</noscript>
    <noscript>{{ 'component-rte.css' | asset_url | stylesheet_tag }}</noscript>
    <noscript>{{ 'disclosure.css' | asset_url | stylesheet_tag }}</noscript>
    <link rel="stylesheet" href="{{ 'custom.css' | asset_url }}">
    <link rel="stylesheet" href="{{ 'newcustom.css' | asset_url }}">
    <!--
      const preloadLink = document.createElement("link");
      preloadLink.href = "custom.css";
        document.head.appendChild(preloadLink);
    -->
    <!--
      <script>
        $(document).on('load',function(){
          console.log('loading');
        var head = document.getElementsByTagName('HEAD')[0];
              var link = document.createElement('link');
              link.rel = 'stylesheet';
              link.type = 'text/css';
              link.href = 'custom.css';
              head.appendChild(link);
          })
      </script>
    -->
    {%- unless settings.type_body_font.system? -%}
      <link rel="preload" as="font" href="{{ settings.type_body_font | font_url }}" type="font/woff2" crossorigin>
    {%- endunless -%}
    {%- unless settings.type_header_font.system? -%}
      <link rel="preload" as="font" href="{{ settings.type_header_font | font_url }}" type="font/woff2" crossorigin>
    {%- endunless -%}
    {%- if settings.predictive_search_enabled -%}
      <link
        rel="stylesheet"
        href="{{ 'component-predictive-search.css' | asset_url }}"
        media="print"
        onload="this.media='all'"
      >
    {%- endif -%}

    <script>
      document.documentElement.className = document.documentElement.className.replace('no-js', 'js');
      if (Shopify.designMode) {document.documentElement.classList.add('shopify-design-mode');}
    </script>

    <script src="{{ 'jquery351.js' | asset_url }}"></script>
    <script src="{{ 'slick.js' | asset_url }}"></script>
    <script src="{{ 'jquery.zoom.js' | asset_url }}"></script>

    <style>
          .product-recommendations ul.grid.product-grid {
            visibility: hidden;
          }
          @font-face {
            font-display: swap;
      font-family: "Manrope 400";
      src: url({{ "Manrope-Regular.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "Manrope 500";
      src: url({{ "Manrope-Medium.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "Manrope 600";
      src: url({{ "Manrope-SemiBold.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "Manrope 700";
      src: url({{ "Manrope-Bold.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "Manrope 800";
      src: url({{ "Manrope-ExtraBold.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "RubikBeastly";
      src: url({{ "RubikBeastly-Regular.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "Times";
      src: url({{ "TIMESR.ttf" | asset_url }});
          }
          @font-face {
            font-display: swap;
      font-family: "bernadette";
      src: url({{ "bernadette.ttf" | asset_url }});
          }
          span.compare_at_price .money {
              text-decoration: line-through;
              margin-right: 6px;
          }
          .add_on_tilte {
              line-height: 17px !important;
          }
    </style>

    <style>
      @media only screen and (min-width:750px) and (max-width:767px){.product .product-page-price{display:block!important;}}
    </style>
  


    {% include 'variable-products-head-template' %}

    <script>
      (() => {const installerKey = 'docapp-discount-auto-install'; const urlParams = new URLSearchParams(window.location.search); if (urlParams.get(installerKey)) {window.sessionStorage.setItem(installerKey, JSON.stringify({integrationId: urlParams.get('docapp-integration-id'), divClass: urlParams.get('docapp-install-class'), check: urlParams.get('docapp-check')}));}})();
    </script>
    <script>
      (() => {const previewKey = 'docapp-discount-test'; const urlParams = new URLSearchParams(window.location.search); if (urlParams.get(previewKey)) {window.sessionStorage.setItem(previewKey, JSON.stringify({active: true, integrationId: urlParams.get('docapp-discount-inst-test')}));}})();
    </script>

    {% comment %} Please do not edit this section. It is automatically generated by Videowise{% endcomment %}
    <link rel="stylesheet" href="https://doui4jqs03un3.cloudfront.net/style.css.gz">
    <script src="https://doui4jqs03un3.cloudfront.net/vendors.js.gz" defer="defer"></script>
    <script src="https://doui4jqs03un3.cloudfront.net/client.js.gz" defer="defer"></script>
    {% comment %} End Videowise section.{% endcomment %}

    {{ shop.metafields.loox.global_html_head }}

    <script type="text/javascript">
      (function(c,l,a,r,i,t,y){
          c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
          t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
          y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "dtnrqoj0ns");
    </script>

    <script type="text/javascript">
      window.heap=window.heap||[],heap.load=function(e,t){window.heap.appid=e,window.heap.config=t=t||{};var r=document.createElement("script");r.type="text/javascript",r.async=!0,r.src="https://cdn.heapanalytics.com/js/heap-"+e+".js";var a=document.getElementsByTagName("script")[0];a.parentNode.insertBefore(r,a);for(var n=function(e){return function(){heap.push([e].concat(Array.prototype.slice.call(arguments,0)))}},p=["addEventProperties","addUserProperties","clearEventProperties","identify","resetIdentity","removeEventProperty","setEventProperties","track","unsetEventProperty"],o=0;o<p.length;o++)heap[p[o]]=n(p[o])};
      heap.load("3719209354");
    </script>

    {% render 'content-header' %}

    

    <script>
      (() => {const installerKey = 'docapp-discount-auto-install'; const urlParams = new URLSearchParams(window.location.search); if (urlParams.get(installerKey)) {window.sessionStorage.setItem(installerKey, JSON.stringify({integrationId: urlParams.get('docapp-integration-id'), divClass: urlParams.get('docapp-install-class'), check: urlParams.get('docapp-check')}));}})();
    </script>

    <script>
      (() => {const previewKey = 'docapp-discount-test'; const urlParams = new URLSearchParams(window.location.search); if (urlParams.get(previewKey)) {window.sessionStorage.setItem(previewKey, JSON.stringify({active: true, integrationId: urlParams.get('docapp-discount-inst-test')}));}})();
    </script>

    <script>
      (() => {const installerKey = 'docapp-discount-auto-install'; const urlParams = new URLSearchParams(window.location.search); if (urlParams.get(installerKey)) {window.sessionStorage.setItem(installerKey, JSON.stringify({integrationId: urlParams.get('docapp-integration-id'), divClass: urlParams.get('docapp-install-class'), check: urlParams.get('docapp-check')}));}})();
    </script>

    <script>
      (() => {const previewKey = 'docapp-discount-test'; const urlParams = new URLSearchParams(window.location.search); if (urlParams.get(previewKey)) {window.sessionStorage.setItem(previewKey, JSON.stringify({active: true, integrationId: urlParams.get('docapp-discount-inst-test')}));}})();
    </script>
    {% comment %}     {% render 'new-breadcrumb' %} {% endcomment %}
  
                      {% include 'bss-product-labels-configs' %}
                        </head>
                 

  <body class="gradient for-font {{ template }} cust-{{ template.name }}  {% if template == 'product' %} template-product {% elsif template == 'index' %} index {% else template == 'cart' %}cart--page{% endif %}{% if template == "collection" %} {{ collection.handle }}{% endif %}">
    <noscript
      ><iframe
        src="https://www.googletagmanager.com/ns.html?id=GTM-W6CWKJR"
        height="0"
        width="0"
        style="display:none;visibility:hidden"
      ></iframe
    ></noscript>
    <a class="skip-to-content-link button visually-hidden" href="#MainContent">
      {{- 'accessibility.skip_to_text' | t -}}
    </a>
    <div class="annoucement-desktop">
      {% section 'announcement-bar' %}
    </div>

    {% section 'header' %}
    {% render 'monogram_popup' %}
    <div id="overlay"></div>
    <main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">
      {{ content_for_layout }}
    </main>

    {% unless template == 'cart' %}
      {%- section 'cart-drawer' -%}
    {% endunless %}
    {% section 'footer' %}

    <ul hidden>
      <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
    </ul>

    {% render 'custom_codes' %}
    {%- if settings.predictive_search_enabled -%}
      <script src="{{ 'predictive-search.js' | asset_url }}" defer="defer"></script>
    {%- endif -%}
    {% if template contains 'product' -%}
      <script id="breadcrumb-script">
        document.addEventListener("DOMContentLoaded",function(){var c=JSON.parse(jQuery(".breadcrumbs-json").html()),a=JSON.parse($("#product-collections").html()),b=1;a.forEach(function(d,e){var a=c.find(a=>a.link==d);a&&(1==b&&setBreadcrumb(a),b++)}),1==b&&a.forEach(function(d,e){var a=c.find(a=>a.parent_link==d);a&&(1==b&&setBreadcrumb(a),b++)}),console.log(b),$(".breadcrumb_text").show()})
      </script>
    {%- endif %}
    {% if template contains 'collection' -%}
      <script id="breadcrumb-script">
        document.addEventListener("DOMContentLoaded",function(){var c=JSON.parse(jQuery(".breadcrumbs-json").html()),d="{{ collection.url }}",a=1,b=c.find(a=>a.link==d);if(b&&(1==a&&setBreadcrumbForCollections(b),a++),1==a){var b=c.find(a=>a.parent_link==d);b&&(1==a&&setBreadcrumbForCollections(b),a++)}console.log(a),$(".breadcrumb_text").show()})
      </script>
    {%- endif %}
    {% render 'custom_codes2' %}

    {% if template contains 'collection' and collection.products.size > 18 %}
      {{ 'tricky3.infinite-scroll.v2.min.js' | asset_url | script_tag }}
      <script>
        $(function(){var a="#product-grid";$(a).t3PageLoad({selectors:{itemsWrapper:a,item:".grid__item",nextPageLink:"#paginatie-next",previousPageLink:"#paginatie-previous",paginationWrapper:".pagination-wrapper"},pageQueryStringKey:"page",waitForImagesToBeLoaded:!1,enablePageState:!0,manualLoading:!1,callBacks:{onAllPagesLoaded:function(){$(".pagination-wrapper, .collection-load-more").css("display","none")},onPageLoad:function(a){$(".collection-load-more").css("display","none")},beforePageLoad:function(){$(".collection-load-more").css("display","flex")}}})})
      </script>
      <style>
        ul#product-grid > div { display: none;}.collection-load-more.loading-overlay__spinner {width: 100%;margin-top: 25px;display: flex;justify-content: center;align-items: center;}.collection-load-more.loading-overlay__spinner svg {width: 35px;}
      </style>
    {% endif %}

    <style>
      .jdgm-ask-question-btn, .jdgm-write-rev-link {border: 1px solid #000000 !important;color : #000000 !important;text-decoration : none !important;font-family: "Manrope 700" !important;Font-Size: 14px !important;}
      #judgeme_product_reviews *:not(.jdgm-star):not(.jdgm-paginate *):not(.jdgm-rev__icon):not(.jdgm-rev__title):not(.jdgm-rev__body p)  { font-family: "Manrope 500" !important;line-height: 21px !important;}
      .jdgm-rev__icon::after, .jdgm-rev__buyer-badge {display: inline-block;color: #ffffff;background-color: black !important;}
      .jdgm-rev__body p { font-family: "Manrope 500" !important;line-height: 21px !important;font-size:14px !important;}
      .jdgm-rev__title  { font-family: "Manrope 500" !important;line-height: 21px !important;font-size:16px !important;}
      .jdgm-paginate * {color:black !important;}
      .jdgm-rev-widg__title {font-size:20px !important;}
      span.jdgm-rev__timestamp, span.jdgm-rev__author, b.jdgm-rev__title, .jdgm-rev-widg__summary-text {font-size: 14px !important;}
    </style>

    <!-- Gorgias Chat Widget Start -->
    <script
      id="gorgias-chat-widget-install-v2"
      src="https://config.gorgias.chat/gorgias-chat-bundle-loader.js?applicationId=18134"
    ></script>
    <script id="gorgias-chat-shopify-install">
      !function(_){_.SHOPIFY_PERMANENT_DOMAIN="{{shop.permanent_domain}}",_.SHOPIFY_CUSTOMER_ID="{{customer.id}}",_.SHOPIFY_CUSTOMER_EMAIL="{{customer.email}}"}(window||{});
    </script>
    <!-- Gorgias Chat Widget End -->

    {{ shop.metafields.loox.global_html_body }}

    <!-- Google Customer Review Badge -->
    <script src="https://apis.google.com/js/platform.js?onload=renderBadge" async defer="defer"></script>
    <script>
      //   window.renderBadge = function() {
      //     var ratingBadgeContainer = document.createElement("div");
      //     document.body.appendChild(ratingBadgeContainer);
      //     window.gapi.load('ratingbadge', function() {
      //       window.gapi.ratingbadge.render(ratingBadgeContainer, {
      //         "merchant_id": 319642804,
      //         "position": "BOTTOM_LEFT"
      //       });
      //     });
      //   }
      //   window.___gcfg = {
      //   lang: '{{ shop_locale.iso_code }}'
      // };
    </script>

    <script>

 $('.product-form__input--dropdown .select__select').on('change', function() {
     event.stopPropagation();
      var size = (this.value);
      var color = $("input[name='Color']:checked").val();
       $(`input[name="Size"][value='`+size+`']`).trigger('click');
      var pJson = JSON.parse($('variant-selects script').html());
      var vrntid = pJson.find(v => (v.option1 == color && v.option2 == size )).id;
      $('input.vrntid').attr('value',vrntid);
      var vrntid = $('select#data [ varsize="'+size+'"][ varcolor="'+color+'"]').attr('var-id');
      setTimeout(function(){
       $('input.vrntid').attr('value',vrntid);
      },1000);
      });
$('.colors__swatches label').click(function(){
       setTimeout(function(){
        var a = $("input[name='Color']:checked").val();
        var b = $('.varsize  .select__select').find(":selected").val();
        var vrntida = $('select#data [ varsize="'+b+'"][ varcolor="'+a+'"]').attr('var-id');
         $('input.vrntid').attr('value',vrntida);
      },1000);  
})

$('cart-remove-button.removes a').click(function(){
   var id = $(this).parents(".sec-tr").attr("data-ids")
    $.ajax({
    type: "POST",
    url: "/cart/change.js",
    dataType: "json",
    data: {
      id: id,
      quantity: 0,
    },
    success:function(data){
        window.location.href="/cart";
    }
  });
});

      $(document).on('click', 'p.optionbuttons', function(){
        var url = $(this).data('url');
        $(this).attr('selected','selected').siblings().removeAttr('selected');
        $.ajax({
            url:"/products/"+url+"",
            success: function(data){
                var parser = new DOMParser();
                doc = parser.parseFromString(data, "text/html");
                var remainder = doc.querySelector(".product-page-banner").innerHTML;
                $(".product-page-banner").replaceWith("<section class=\"shopify-section section product-page-banner\">" + remainder + "</section>");
                history.pushState(null, '', '/products/'+url+'');            
            }
        });
      });

    $(document).on('click','.drop_down-arrow',function(){
       $(this).hide();
 $('.drop_up-arrow').show();
       $('.refdiv.refdiv_toggle').css("display", "grid");
    })

     $(document).on('click','.drop_up-arrow',function(){
       $(this).hide();
      $('.drop_down-arrow').show();
       $('.refdiv.refdiv_toggle').css("display", "none");
    })

$(document).on('click','.monogram_check input[type="checkbox"]', function () {
    var proid = $(this).parents('.cart-item').data('proid');
    var item = $(this).parents('.cart-item').data('item_id');
    var color = $(this).parents('.cart-item').find('.cart-item_color').attr('data-clr');
    var sku_product = $(this).parents('.cart-item').data('sku');
    var url = "https://oxaleather.com/search?view=monogram_view&q=id:"+proid+"&type=product&variant="+item
    $.ajax({
        type: "GET",
        url: url,
        success: function (msg) {
            var parser = new DOMParser();
            doc = parser.parseFromString(msg, "text/html");
            var remainder = doc.querySelector('[varid="'+item+'"]').innerHTML;
            var monoimgg = doc.querySelector('img.hideimagesvar[data-color="'+color+'"][alt*="monogram"]').getAttribute("src");
            $('.custommodal .modal-content.mono-radius').html(remainder);
            var mono_top = $('.custommodal .main-img-monogram_pop').data("top");
            var mono_left = $('.custommodal .main-img-monogram_pop').data("left");
            var mono_size = $('.custommodal .main-img-monogram_pop').data("size");
            $('.custommodal .main-img-monogram img').attr('src',monoimgg)
            $('.custommodal .add-to-cart button').attr('sku',sku_product)

            window.img_width = window.img_height = "";
            window.pop_stage = new Konva.Stage({
              container: '.main-img-monogram_pop',
              width: 400,
              height: 400
            });
            
            var layer = new Konva.Layer();
            pop_stage.add(layer);
            
            var imageObj = new Image();
            imageObj.onload = function () {
            window.img_width = imageObj.width; window.img_height = imageObj.height;
            var yoda = new Konva.Image({
              id: "mono_image",
              x: 0,
              y: 0,
              image: imageObj,
              width: 400,
              height: 400,
            });

            layer.add(yoda);
            var tt = $("#pop_name").val();
            var ff = $("span.right-click.righticon").parent().data("font");
            var simpleText = new Konva.Text({
              id: "mono_text",
              x: mono_left,
              y: mono_top,
              width:200,
              text: tt,
              fontSize: mono_size,
              fontFamily: ff,
              fontStyle: 'bold',
              fill: 'black',
              verticalAlign: 'middle',
              align: 'center',
              wrap: "word"
            });

          layer.add(simpleText);
        };
        imageObj.crossOrigin = 'Anonymous';
        imageObj.src = monoimgg;
            $('.custommodal#exmpModalToggleone').addClass('show');
            $('.custommodal#exmpModalToggleone').show();
            $(".custommodal .fonts-select .times").find(".right-click").addClass('righticon');
          $(document).on('input', '.custommodal .name-initial input', function(){
              var tx = window.pop_stage.findOne("#mono_text");
              tx.text($(this).val());
          })
        }
    });
});
document.addEventListener("DOMContentLoaded", function(event) {
    $(document).on('click','.monogram-desk .canclebtn',function(){
        $('#exmpModalToggleone').removeClass('show');
        $('#exmpModalToggleone').hide();
        $('.monogram_check input[type="checkbox"]').prop('checked', false);
    })
});
$(document).on('click','.monogram-desk .canclebtn',function(){
    $('#exmpModalToggleone').removeClass('show');
    $('#exmpModalToggleone').hide();
    $('.monogram_check input[type="checkbox"]').prop('checked', false);
})
$('.monogram_check').each(function(){
    var str1 = $(this).data('tags');
    var str2 = "notcustom";
    if(str1.indexOf(str2) != -1){
      $(this).addClass('hidemono');
    }
})

    </script>

{% comment %}
<script>
   
$.get("https://ipinfo.io", function(response) {
    if (response.country === "US") {
      $(".free-shipping-label").show();
    }
  }, "jsonp");

$(document).ready(function() {
      $('.product-form__input input[checked]').each(function() {
        $(this).click();
      });
  });
</script>
 {% if product.metafields.custom.upgrades.value != blank %}
<script>
$(document).on("click",'.product-form__submit',function(e) {

    e.preventDefault();

    var mf = [];
    var st = $(this).parents('form[data-type="add-to-cart-form"]').find('.vrntid').val();
    $('.addons.main-product').attr('data-id', st);

    $(".addons").each(function () {
        var checkbox = $(this).find('input[type="checkbox"]');
        var isChecked = checkbox.is(":checked");

        if (isChecked) {
            var var1 = $(this).attr('data-id');
            mf.push({
                id: var1,
                quantity: 1
            });
        } else if ($(this).hasClass('main-product')) {
            var var1 = $(this).attr('data-id');
            mf.push({
                id: var1,
                quantity: 1
            });
        }

    });
console.log(mf)
    $.ajax({
        type: 'POST',
        url: '/cart/add.js',
        data: { items: mf },
        dataType: 'json',
        success: function (cart) {
            console.log('success');
            cart_drawer.$data.cart_items = cart.items;
            cart_drawer.$data.cart_open = true;
        }
    });
});

</script>
{% endif %}{% endcomment %}
      <script>
      
            $(document).on('click','.mega-menus nav.nav_menu_custom ul.mega-list-menu a:first-child',function(){
                var parent = $(this).attr('href');
                localStorage.setItem('parentlink',parent);
                var handl = $(this).attr('collname')
                localStorage.setItem('parentColl',handl);
                localStorage.removeItem('botomscol');
            });
           $(document).on('click','li.new_custom_menu',function(){
                var parent2 = $(this).parents('.list_section_desktop').find('.parentdiv').attr('href');
                localStorage.setItem('parentlink',parent2);
                var handl2 = $(this).parents('.list_section_desktop').find('.parentdiv').attr('collname');
                localStorage.setItem('parentColl',handl2);
                localStorage.removeItem('botomscol');
            })
          $(document).ready(function(){
             if (window.location.href.indexOf("/collections/") > -1) {
                $('.lgrid').click(function(){
                    var subcolurl = $(this).attr('url');
                    var subcol = $(this).attr('parentcol');
                    var temp = `<li class="breadcrumbs__item"><a class="breadcrumbs__link" href="`+subcolurl+`">`+subcol+`</a></li>')`;
                    localStorage.setItem('botomscol',temp);
                })
                 var pname = localStorage.getItem('parentColl');
                 var plink = localStorage.getItem('parentlink');
               $('#product-grid li.grid__item').click(function(){
                    var lastelm = $('ol.breadcrumbs__list').html();
                    localStorage.setItem('probread',lastelm);
               })
               if(localStorage.getItem('botomscol')){
                   var newitm = localStorage.getItem('botomscol');
                   $(newitm).insertAfter("ol.breadcrumbs__list .breadcrumbs__item:first-child");
                   //$('ol.breadcrumbs__list li.breadcrumbs__item:nth-child(1)').remove();
               }else{
                  var path = window.location.pathname;
                 if(path == plink){
                   
                 }else{
                   if(pname){
                      $('<li class="breadcrumbs__item"><a class="breadcrumbs__link" href="'+plink+'">'+pname+'</a></li>').insertAfter("ol.breadcrumbs__list .breadcrumbs__item:first-child");
                      //$('ol.breadcrumbs__list li.breadcrumbs__item:nth-child(1)').remove();
                   }
                 }
               }
               
            }
            if(window.location.href.indexOf("/products/") > -1){
              if(localStorage.getItem('probread')){
                  $('ol.breadcrumbs__list li:not(:last)').remove();
                  const h2 = document.querySelector("ol.breadcrumbs__list");
                  let html = localStorage.getItem('probread');
                  h2.insertAdjacentHTML("afterbegin", html);
                  //$('ol.breadcrumbs__list li:nth-child(1)').remove();
                  $('ol.breadcrumbs__list li:nth-child(1) a').removeAttr('aria-current');
                  $('ol.breadcrumbs__list li:nth-child(2) a').removeAttr('aria-current');
                  $('ol.breadcrumbs__list li:nth-child(3) a').removeAttr('aria-current');
               }
            }


          ////////////////////////////////// script ///////////////////////////


          var schma = '';
          var i = 1;
          var total = $('ol.breadcrumbs__list li').length;
          $('ol.breadcrumbs__list li').each(function(e){
              var link = $(this).find('a').attr('href')
              var titl = $(this).find('a').text();
              if (e === total - 1) {
                  schma += `{
                  "@type": "ListItem", 
                  "position": `+i+++`, 
                  "name": "`+titl+`",
                  "item": "`+link+`"  
                  }`;
              } else {
                  schma += `{
                  "@type": "ListItem", 
                  "position": `+i+++`, 
                  "name": "`+titl+`",
                  "item": "`+link+`"  
                  },`;
              }
          })
          var scrpt = `<script type="application/ld+json">
          {
          "@context": "https://schema.org/", 
          "@type": "BreadcrumbList", 
          "itemListElement": [`+schma+`]
          }`;
          $('body').append(scrpt);
          $('body.product ol.breadcrumbs__list').show();
          $('body.collection ol.breadcrumbs__list').show();
      })
      $('body.cust-page').click(function(){
           localStorage.removeItem('probread')
           localStorage.removeItem('parentColl');
           localStorage.removeItem('parentlink'); 
      });
      $('body.cust-index').click(function(){
           localStorage.removeItem('probread');
           localStorage.removeItem('parentColl');
           localStorage.removeItem('parentlink'); 
      });
      //  $(document).on('click', '.name-initial input#name', function(){
      // $('.fonts-select .times').trigger('click');
      //  })

       //  $(document).on('click', '.name-initial input#name:focus', function(){
       //  $('.fonts-select .times').click();
       // })
    </script>
    {% comment %}
      <script>
        setInterval(function(){
            $('#WebPixelsManagerSandboxContainer').remove();
        },200)
      </script>
    {% endcomment %}

<script>
$(document).ready(function() {
      $('.product-form__input input[checked]').each(function() {
        $(this).click();
      });
 });

{% comment %} setTimeout(function() { 
$('.color__swatches').each(function() {
      if (!$(this).children().hasClass('active-swatch')) {
        $(this).children().first().click();
      }
    });
},4500); {% endcomment %}
</script>

 {% if product.metafields.custom.upgrades.value != blank %}
<script>

$(document).on("click",'.product-form__submit',function(e) {

    e.preventDefault();
    var mf = [];
    var st = $(this).parents('form[data-type="add-to-cart-form"]').find('.vrntid').val();
    $('.addons.main-product').attr('data-id', st);
    $(".addons").each(function () {
        var checkbox = $(this).find('input[type="checkbox"]');
        var isChecked = checkbox.is(":checked");
        if (isChecked) {
            var var1 = $(this).attr('data-id');
            mf.push({
                id: var1,
                quantity: 1
            });
        } else if ($(this).hasClass('main-product')) {
            var var1 = $(this).attr('data-id');
            var pri1 = $(this).attr('data-price');
            var sym1 = $(this).attr('data-symbol');
            mf.push({
                id: var1,
                quantity: 1
            });
        }
    });
console.log(mf)
    $.ajax({
        type: 'POST',
        url: '/cart/add.js',
        data: { items: mf },
        dataType: 'json',
        success: function (cart) {
            console.log('success');
            cart_drawer.$data.cart_items = cart.items;
            cart_drawer.$data.cart_open = true;
        }
    });
});

// function upgrades(button) {
//     // You can use the 'button' parameter if you need to access the clicked button
//     console.log('Button clicked!');

//     // Your existing code starts here
//     var mf = [];
//     var st = $(button).parents('form[data-type="add-to-cart-form"]').find('.vrntid').val();
//     $('.addons.main-product').attr('data-id', st);
//     $(".addons").each(function () {
//       var checkbox = $(this).find('input[type="checkbox"]');
//       var isChecked = checkbox.is(":checked");
//       if (isChecked) {
//         var var1 = $(this).attr('data-id');
//         mf.push({
//           id: var1,
//           quantity: 1
//         });
//       } else if ($(this).hasClass('main-product')) {
//         var var1 = $(this).attr('data-id');
//         var pri1 = $(this).attr('data-price');
//         var sym1 = $(this).attr('data-symbol');
//         mf.push({
//           id: var1,
//           quantity: 1
//         });
//       }
//     });
//     console.log(mf);
//     $.ajax({
//       type: 'POST',
//       url: '/cart/add.js',
//       data: { items: mf },
//       dataType: 'json',
//       success: function (cart) {
//         console.log('success');
//         cart_drawer.$data.cart_items = cart.items;
//         cart_drawer.$data.cart_open = true;
//       }
//     });
//   }

</script>
<script>

document.addEventListener("DOMContentLoaded", (event) => {
  document.querySelector('variant-radios').dispatchEvent(new Event('change'));
});
     $(document).on('change', '.addons-upgrades',function() {
   document.querySelector('variant-radios').dispatchEvent(new Event('change'));
       return true
    if ($(this).prop('checked')) {
     var $this = $(this)
      var price1 = $this.attr('data-prices');
  alert(price1);
      var symbol1 = $this.attr('data-symbols');
  
    var tot = $this.parents('.other_section_color').find('.price__sale.aa span.price-item.price-item--sale.price-item--last').attr('data-price-1');  
        
       alert(tot);
      var result = parseFloat(price1) + parseFloat(tot);
      alert(result);
      var totalprice= symbol1 + result.toFixed(2);
     $this.parents('.other_section_color').find('span.price-item span').text(totalprice);
      $this.parents('.other_section_color').find('span.price-item').attr('data-price',result.toFixed(2));

       }
else {
var price2 = parseFloat($(this).attr('data-prices'));
            var symbol2 = $(this).attr('data-symbols');
            var tot1 = parseFloat($(this).parents('.other_section_color').find('.price__sale.aa span.price-item.price-item--sale.price-item--last').attr('data-price-1'));
            var result1 = tot1 - price2;
            var totalprice1 = symbol2 + result1.toFixed(2);
            $(this).parents('.other_section_color').find('span.price-item span').text(totalprice1);
            $(this).parents('.other_section_color').find('span.price-item').attr('data-price', result1.toFixed(2));
    }
   
  });

$('.product-form__input label1').click(function(){
  
    $('.addons-upgrades').prop('checked', false);
    
            var symbol1 = $('.addons-upgrades').attr('data-symbols');
            var tot = parseFloat($(this).parents('.other_section_color').find('.price__sale.aa span.price-item.price-item--sale.price-item--last').attr('data-price-1'));
            var totalprice = symbol1 + tot;
            $(this).parents('.other_section_color').find('span.price-item span').text(totalprice);
            $(this).parents('.other_section_color').find('span.price-item').attr('data-price', tot);
 
})
</script>
{% endif %}
<script>
$(document).ready(function(){
$.get("https://ipinfo.io", function(response) {
    if (response.country === "US") {
      $(".free-shipping-label").show();
    }
    else{
       $(".free-shipping-label").hide();
    }
  }, "jsonp");
})
document.addEventListener("DOMContentLoaded", (event) => {
  $.get("https://ipinfo.io", function(response) {
    if (response.country === "US") {
      $(".free-shipping-label").show();
    }
    else{
       $(".free-shipping-label").hide();
    }
  }, "jsonp");
});
// $(document).on('click', '.product-form__input label[data-color*="color"]',function() {
// $.get("https://ipinfo.io", function(response) {
//     if (response.country === "US") {
//       $(".free-shipping-label").show();
//     }
//   }, "jsonp");
// })
</script>

    {% render 'content-footer' %}
    {% render 'preorder-now' %}
    {% unless template contains 'cart' %}
      <script src="https://rapidlercdn.com/6426631c44f2512c1cbbd64f/deferload.js" defer></script>
      <a href="https://rapidler.com/" style="display: none;">Rapidler Shopify Speed Optimization Agency</a>
    {% endunless %}
  {% if content_for_header contains 'product_label' %}{% include 'bss-product-label-js' %}{% include 'bss-label-style-css' %}{% include 'bss-product-label-fonts' %}{% endif %}</body>
</html>